{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '--- Starting Migration ---' && npx prisma migrate deploy && echo '--- Migration Done, Checking Files ---' && ls -R dist/src && echo '--- Starting Node Server ---' && node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE"
  }
}